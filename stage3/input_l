begin
i = 0;
while(i < 1) do
	j = 0;
	while(j < 2) do
		k = 0;
		while( k < 2) do
			write("(");
			write(i);
			write(", ");
			write(j);
			write(", ");
			write(k);
			write(")");
			k = k+1;
		endwhile;
		j = j + 1;
	endwhile;
	
	i = i + 1;
	
endwhile;

end